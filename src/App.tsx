import React, { Fragment } from 'react';

import { Dashboard } from '@/features/dashboard/Dashboard';

const App: React.FC = () => {
	return (
		<Fragment>
			<Dashboard />
		</Fragment>
	);
};

export default App;
