{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Install Dependencies",
			"type": "shell",
			"command": "pnpm install && pnpm install --prefix proxy-server",
			"options": {
				"cwd": "${workspaceFolder}",
				"statusbar": {
					"label": "$(package) Install Dependencies",
					"running": {
						"color": "#4CAF50",
						"label": "$(sync) Installing Dependencies..."
					},
					"detail": "Install Project Dependencies (Frontend + Proxy)"
				}
			}
		},
		{
			"label": "Start Frontend",
			"type": "shell",
			"command": "pnpm run start",
			"options": {
				"cwd": "${workspaceFolder}",
				"statusbar": {
					"label": "$(run) Start Frontend",
					"running": {
						"color": "#22C1D6",
						"label": "$(triangle-right) App is Running"
					},
					"detail": "Start Frontend"
				}
			},
			"isBackground": true,
			"problemMatcher": []
		},
		{
			"label": "Start Proxy Server",
			"type": "shell",
			"command": "node server.js",
			"options": {
				"cwd": "${workspaceFolder}/proxy-server",
				"statusbar": {
					"label": "$(run) Start Proxy Server",
					"running": {
						"color": "#22d622",
						"label": "$(triangle-right) Proxy Server is Running"
					},
					"detail": "Start Proxy Server"
				}
			},
			"isBackground": true,
			"problemMatcher": []
		},
		{
			"label": "Start Frontend + Proxy Server",
			"type": "shell",
			"command": "echo Starting Frontend + Proxy Server...",
			"dependsOn": ["Start Frontend", "Start Proxy Server"],
			"dependsOrder": "parallel",
			"isBackground": true,
			"problemMatcher": [],
			"options": {
				"statusbar": {
					"label": "$(ruby) Start Frontend + Proxy Server",
					"running": {
						"color": "#f7ca02",
						"label": "$(triangle-right) Started Frontend + Proxy Server"
					},
					"detail": "Start Frontend + Proxy Server"
				}
			}
		},
		{
			"label": "Lint",
			"type": "shell",
			"command": "pnpm run lint",
			"options": {
				"cwd": "${workspaceFolder}",
				"statusbar": {
					"label": "$(debug) Lint",
					"detail": "Lint"
				}
			},
			"isBackground": true,
			"problemMatcher": []
		},
		{
			"label": "Type-check",
			"type": "shell",
			"command": "pnpm run type-check",
			"options": {
				"cwd": "${workspaceFolder}",
				"statusbar": {
					"label": "$(callstack-view-session) Type-check",
					"detail": "Type-check"
				}
			},
			"problemMatcher": "$tsc"
		},
		{
			"label": "Build",
			"type": "shell",
			"command": "pnpm run build",
			"options": {
				"cwd": "${workspaceFolder}",
				"statusbar": {
					"label": "$(gift) Build",
					"detail": "Build"
				}
			},
			"isBackground": true,
			"problemMatcher": []
		}
	]
}
